{
  "name": "burak",
  "version": "1.0.0",
  "description": "Burak project backend files",
  "license": "ISC",
  "author": "Doston ADIKHAMOV",
  "type": "commonjs",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "train": "nodemon --exec ts-node train.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "moment": "^2.30.1"
  }
}
